import { SortSprintPipe } from './sort-sprint.pipe';

describe('SortSprintPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSprintPipe();
    expect(pipe).toBeTruthy();
  });
});
