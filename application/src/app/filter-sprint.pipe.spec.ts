import { FilterSprintPipe } from './filter-sprint.pipe';

describe('FilterSprintPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSprintPipe();
    expect(pipe).toBeTruthy();
  });
});
